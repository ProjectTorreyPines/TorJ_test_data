name: Release Data

on:
  push:
    branches: ["master"]

jobs:
  test:
    uses: ProjectTorreyPines/GitHubActionsWorkflows/.github/workflows/artifact_release.yaml@master
    secrets: inherit
    with:
      data_path: "data"